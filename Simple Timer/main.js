const spanEl = document.querySelector("span");
const playBtnEl = document.querySelector(".fa-play");




playBtnEl.addEventListener("click", ()=>{
    function updateSpan()
})

function updateSpan() {
    let timer = setTimeout
    spanEl.innertext = timer;
}

setTimeout(()=>{ updateSpan()

},1000)

updateSpan()